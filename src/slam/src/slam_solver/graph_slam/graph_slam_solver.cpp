#include "slam_solver/graph_slam/graph_slam_solver.hpp"

// void GraphSLAMSolver::add_prior_pose(const common_lib::structures::Velocities& velocities) {
//   // Create a new prior factor
//   //   PriorFactor<Pose2> prior_factor(x1, Pose2(0, 0, 0));
//   //   // Add the prior factor to the graph
//   //   graph.push_back(prior_factor);
// }