#ifndef SRC_PLANNING_INCLUDE_UTILS_ANGLE_AND_NORM_HPP_
#define SRC_PLANNING_INCLUDE_UTILS_ANGLE_AND_NORM_HPP_

/**
 * @brief Struct to store the angle and the norm of a vector
 * 
 */
struct AngleNorm {
    double angle;
    double norm;
};

#endif