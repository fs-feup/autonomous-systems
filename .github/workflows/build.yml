name: Testing and Building

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  perception:
    runs-on: ubuntu-latest

    container:
      image: ros:humble-ros-base-jammy

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: Install dependencies
        run: |
          echo "source /opt/ros/$ROS_DISTRO/setup.bash" >> /home/$USERNAME/.bashrc
          ./dependencies_install.sh
      
      - name: Rosdep
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          rosdep update
          rosdep install --from-paths src --ignore-src -r -y
      
      - name: Build Dependencies
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          colcon build --packages-select custom_interfaces eufs_msgs fs_msgs common_lib

      - name: Build and Test Perception
        run: |
          source install/setup.bash
          colcon build --packages-select perception
          colcon test --packages-select perception --event-handler=console_direct+

  ekf_state_est:
    runs-on: ubuntu-latest

    container:
      image: ros:humble-ros-base-jammy

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: Install dependencies
        run: |
          echo "source /opt/ros/$ROS_DISTRO/setup.bash" >> /home/$USERNAME/.bashrc
          ./dependencies_install.sh
      
      - name: Rosdep
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          rosdep update
          rosdep install --from-paths src --ignore-src -r -y
      
      - name: Build Dependencies
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          colcon build --packages-select custom_interfaces eufs_msgs fs_msgs common_lib

      - name: Build and Test Localization and Mapping
        run: |
          source install/setup.bash
          colcon build --packages-select ekf_state_est
          colcon test --packages-select ekf_state_est --event-handler=console_direct+

      
  control:
    runs-on: ubuntu-latest

    container:
      image: ros:humble-ros-base-jammy

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: Install dependencies
        run: |
          echo "source /opt/ros/$ROS_DISTRO/setup.bash" >> /home/$USERNAME/.bashrc
          ./dependencies_install.sh
      
      - name: Rosdep
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          rosdep update
          rosdep install --from-paths src --ignore-src -r -y
      
      - name: Build Dependencies
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          colcon build --packages-select custom_interfaces eufs_msgs fs_msgs common_lib

      - name: Build and Test Control
        run: |
          source install/setup.bash
          colcon build --packages-select control
          colcon test --packages-select control --event-handler=console_direct+


  planning:
    runs-on: ubuntu-latest

    container:
      image: ros:humble-ros-base-jammy

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: Install dependencies
        run: |
          echo "source /opt/ros/$ROS_DISTRO/setup.bash" >> /home/$USERNAME/.bashrc
          ./dependencies_install.sh
      
      - name: Rosdep
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          rosdep update
          rosdep install --from-paths src --ignore-src -r -y
      
      - name: Build Dependencies
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          colcon build --packages-select custom_interfaces eufs_msgs fs_msgs common_lib

      - name: Build and Test Planning
        run: |
          source install/setup.bash
          colcon build --packages-select planning
          colcon test --packages-select planning --event-handler=console_direct+


  inspection:
    runs-on: ubuntu-latest

    container:
      image: ros:humble-ros-base-jammy

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: Install dependencies
        run: |
          echo "source /opt/ros/$ROS_DISTRO/setup.bash" >> /home/$USERNAME/.bashrc
          ./dependencies_install.sh
      
      - name: Rosdep
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          rosdep update
          rosdep install --from-paths src --ignore-src -r -y
      
      - name: Build Dependencies
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          colcon build --packages-select custom_interfaces eufs_msgs fs_msgs common_lib

      - name: Build and Test Inspection Node
        run: |
          source install/setup.bash
          colcon build --packages-select inspection
          colcon test --packages-select inspection --event-handler=console_direct+

  mocker_node:
    runs-on: ubuntu-latest

    container:
      image: ros:humble-ros-base-jammy

    defaults:
      run:
        shell: bash

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'true'

      - name: Install dependencies
        run: |
          echo "source /opt/ros/$ROS_DISTRO/setup.bash" >> /home/$USERNAME/.bashrc
          ./dependencies_install.sh
      
      - name: Rosdep
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          rosdep update
          rosdep install --from-paths src --ignore-src -r -y
      
      - name: Build Dependencies
        run: |
          source /opt/ros/$ROS_DISTRO/setup.bash
          colcon build --packages-select custom_interfaces eufs_msgs fs_msgs common_lib

      - name: Build and Test Mocker Node
        run: |
          source install/setup.bash
          colcon build --packages-select mocker_node
          colcon test --packages-select mocker_node --event-handler=console_direct+


